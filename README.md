# Completed Matches of FIFA WORLD CUP - 2026

1. Install the selenium library with pip package manager.<br>
   ```pip install selenium==4.43.0```

2. Make sure Edge browser is installed. For other platforms (like Linux, MacOS) use another browser's driver.
3. Run the Python code.<br>
```py fifa.py```
